import React from "react";

const Header = () => {
  return (
    <div>
      <h1 className="header">Profile Card</h1>
    </div>
  );
};

// Named Export /Import
// default Export /Import

export default Header;
